apiVersion: fluidos.eu/v1
kind: ModelBasedDeployment
metadata:
  name: nginx
spec:
  apiVersion: v1
  kind: Pod
  metadata:
    name: nginx
  spec:
    containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
      - containerPort: 80
